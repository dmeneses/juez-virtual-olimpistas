
/**
 * Compile and run the given files in attempt.
 * If a error is produced will be set the variables status and errorMessage in attempt.
 * In other case the grade result will be set in attempt.
 */
void grade(SolutionAttempt& attempt) 